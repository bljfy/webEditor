# 01 系统定位

## 概述
本系统是“Schema 驱动的配置化页面生成系统”，核心流程是通过 Panel 生成 `PageConfig`，再由 Renderer 确定性渲染页面并支持导出。

## 功能与职责
- 定义系统目标：结构化编辑、实时预览、离线导出单页 HTML。
- 定义目标用户：面向不懂代码的普通用户，核心操作必须可通过可视化交互完成。
- 定义系统非目标：非自由排版工具、非 HTML/CSS 编辑器、非 Figma/Webflow 式所见即所得。
- 为后续模块提供边界前提，避免需求漂移。
